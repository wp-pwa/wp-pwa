/* eslint-disable import/prefer-default-export */
import settings from '../packages/settings/sagas/client';
import build from '../packages/build/sagas/client';

export default {
  settings,
  build,
}
